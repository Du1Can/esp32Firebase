import 'dart:io';
void main(List<String> args) {
  /*String? vallor;
  vallor = stdin.readLineSync()!; //ponerle el ! puede arreglar el error. otra forma es validarlo
  int valorint = int.parse(vallor);
  int valor;
  valor = int.parse(stdin.readLineSync()!);*/
  int nValor;
  nValor = int.parse(stdin.readLineSync()!);
  print("Ingresaste: $nValor");

}