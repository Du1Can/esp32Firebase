class Data{
  late DateTime fecha;
  late double humedad;
  late double temperatura;

  Data(
      this.fecha,
      this.humedad,
      this.temperatura,
      );

//late es para  decirle que despues inicializamos la variable.
}